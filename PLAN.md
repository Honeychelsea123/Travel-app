# 여행앱 v2 — 계획

2026-08-02 시작. 도쿄 개인용 PWA(`../pwa/`)는 9/12 여행 때까지 지금 상태로 두고,
여기서 여러 사람이 쓰는 앱을 새로 만든다.

근거 문서: `Downloads/SUPABASE-MIGRATION.md` (제품 방향과 결정 목록),
`../HANDOFF.md` (지금 구조와 알아낸 제약).

## 정한 것

| 항목 | 결정 |
|---|---|
| 협업 | 여행 하나를 여러 명이 같이 편집. 초대 코드, owner/editor/viewer |
| 백엔드 | Supabase (계정 있음, 프로젝트는 새로 만듦) |
| 프론트 | 단일 HTML + CDN. 빌드 없음 |
| 배포 | **새 저장소 `Honeychelsea123/Travel-app`** → `honeychelsea123.github.io/Travel-app/` |
| 도시 | 전 세계. 도쿄 전용 상수는 `cities` 기본값 + `trips` 행으로 뺐다 |
| 로그인 | 구글 하나로 시작 (문서 "계정" 절) |

아직 안 정한 것: 무료·유료 경계. `ai_usage` 구조는 만들어 뒀으니 숫자만 꽂으면 된다.
문서 8-1의 제안은 무료 15회/일, 여행 기간 30회, 유료 100회.

## 도쿄 앱을 건드리지 않기 위한 것

저장소가 다르니 파일은 안 섞인다. 단 **같은 `github.io` 도메인이라
localStorage 와 IndexedDB 는 공유된다.** 서비스워커 스코프는 `/PWA/`와
`/Travel-app/`으로 갈리지만 저장소는 안 갈린다.
→ **v2 의 모든 저장 키에 `t2:` 접두사를 붙인다.** 도쿄 앱 키를 덮어쓰면 여행 중에 터진다.

## 왜 단일 HTML 인가

이 컴퓨터에 node/npm 이 없다. git 만 있다. 도쿄 앱이 이미 단일 HTML 로 잘 돌고,
오프라인 구조(캐시 우선 SW, 쓰기 큐)를 그대로 옮길 수 있다.
파일이 커지면 화면 단위로 ES 모듈로 쪼갠다 — 빌드 없이 된다.

## 단계

### 9장 1번 — 프로젝트와 스키마 (2026-08-02 완료)

- [x] 스키마와 RLS (`db/001_schema.sql`)
- [x] 도시·노선색 시드 (`db/002_seed.sql`)
- [x] 저장소 생성 — `Honeychelsea123/Travel-app`
- [x] Supabase 프로젝트 — `qahqqhjleqfrsjiixnas`, Seoul
- [x] `db/003_verify.sql` 8줄 전부 OK (표 19 · RLS 전부 · 정책 35 · 시드 22/40)

### 나라·도시 확장 (2026-08-02 완료)

- [x] `countries` 56개 — 통화·언어·맛집 사이트는 나라의 성질이라 여기로 옮겼다
- [x] `cities` 22 → 142개, `transit_grades` 4등급 (`db/004_countries_cities.sql`)
- [x] `db/005_verify_cities.sql` 9줄 전부 OK — 시간대 142개가 실재하는 IANA 이름임을
      `pg_timezone_names` 로 직접 확인. 통화도 전부 나라 값과 일치

### 연결 (2026-08-02 완료)

- [x] GitHub Pages 켜짐 → https://honeychelsea123.github.io/Travel-app/
- [x] publishable key 로 연결. `index.html` 이 로그인 없이 `cities`·`countries` 를 읽는다
- [x] 로그인 안 한 상태에서 `trips` 가 비어 있는 것 확인 — RLS 가 실제로 일하고 있다는 증거
- [x] 도쿄 앱 무사 확인 — `BUILD b68`, 작업트리 0건, Supabase 흔적 없음

### 구글 로그인 — 9장 4번 (2026-08-02 완료)

- [x] Google Cloud 프로젝트 `travel-app-504306` + OAuth 클라이언트(웹)
- [x] Supabase Google 활성화. **구글만 켜고 email 포함 24개는 껐다**
- [x] Site URL `.../Travel-app/`, Redirect URLs `.../Travel-app/**`
- [x] 로그인 성공. `profiles`·`user_prefs` 자동 생성 확인 — 가입 트리거가 실제로 돈다

구글 콘솔 값:

| 칸 | 값 |
|---|---|
| 승인된 JavaScript 원본 | `https://honeychelsea123.github.io` |
| 승인된 리디렉션 URI | `https://qahqqhjleqfrsjiixnas.supabase.co/auth/v1/callback` |

**동의 화면이 Testing 상태면 테스트 사용자에 등록된 계정만 로그인된다.**
남에게 열 때 Publish 를 누른다. 이름·이메일·사진만 받는 기본 범위라 구글 심사는 없다.

**켜진 로그인 방법은 이걸로 확인한다** (대시보드를 안 열어도 된다):
`GET /auth/v1/settings` 에 `apikey` 헤더만 붙이면 provider 목록이 그대로 온다.

### 여행 · 일정 · 일행 (9장 2·3번, 2026-08-02)

- [x] 새 여행 — 도시 검색(한국어·영어·현지표기·나라·초성), 목록에 없으면 친 그대로
- [x] 삭제 · 보관 · 나가기. owner 만 삭제, 일행은 나가기
- [x] 여행 정보 수정 + **일정 전체 미루기** (`shift_trip_days` RPC, `008`)
- [x] 여행 상세 · 날짜 칩 · 일정 추가/삭제(소프트)
- [x] 초대 링크 · 일행 목록 · 권한 변경 · 내보내기 · 별명 (`009`)
- [ ] **다른 계정으로 초대 수락 실기기 확인** — 아직 안 해봄
- [ ] 실시간 구독 (9장 5번)
- [ ] 지출 · 예약 · 준비물 · 링크 화면
- [ ] 디자인 이식 (도쿄 앱 토큰 · 분류색 7개 · 노선색 40개)

### 그 뒤로 붙인 것 (2026-08-02)

- **실시간** — 일정·지출·일행·구간. 도쿄 앱의 8초 폴링을 대체
- **구간(`trip_legs`)** — 여행 하나가 여러 도시·나라. 경계는 그날 밤 자는 곳
- **환율** — 쓴 날 값을 `fx_rate`·`amount_home`에 못박음. 정산은 집 통화 하나로
- **준비 탭** — 예약(예약번호)·준비물(담당)·링크
- **AI** — Edge Function 뒤에 Gemini. 대화 · 제안 카드(좌표 포함) · 사용량 한도
- **일정 검토** — 겹침·과밀·빈 날·이른 체크인·**이동 시간**. 계산이라 공짜
- **여행지 기록** — 아래

### 여행지 기록 — 왓챠피디아 참고 (2026-08-02)

**일정 앱은 1년에 두 번 열린다.** 돌아올 이유를 만드는 자리다.

- 도시 별점(0.5 단위) · 가보고 싶어요 · 평균(인원수 포함) · 한 줄평
- 도시 상세 — 사진 · 위키백과 요약 · 대륙/통화/시간대/이동 · **내가 거기서 뭘 했는지**
- 여행 후기 — 끝난 여행에 별점 + 한 줄, 그 여행 도시들을 바로 평가
- 나라 87 · 도시 313 · 사진 313(Pexels) · 설명 298(위키백과)
- 없는 도시는 사용자가 직접 넣는다. 나라만 고르면 나머지는 따라온다

**추천과 예상 별점은 만들지 않기로 했다.** 근거보다 세게 들리고, 여행은 틀렸을 때
대가가 영화와 다르다. 남들 평균은 사실이라 보여주되 몇 명이 매겼는지 같이 단다.

**"가봤어요"는 저장하지 않고 계산한다** — 별점이 있거나 지난 여행의 구간 도시.
스위치로 뒀다가 별점을 지워도 남는 버그가 났다. 스위치가 없으면 어긋날 자리도 없다.

**한 줄평만 이름이 공개된다.** 별점만 매기면 익명 집계에만 들어간다.

**사진은 Pexels, 글은 위키백과.** 사진은 상업적 사용이 되고 출처 표기 의무가 없지만
**위키백과 글은 CC BY-SA라 출처를 반드시 단다.**

### 겪은 함정 (되풀이하지 말 것)

**`window.confirm` · `prompt` 를 쓰지 않는다.** 내장 브라우저나 iframe 안에서 조용히
막혀 `false`/`null` 을 돌려준다. 그러면 요청조차 안 나가고 아무 말도 없다.
삭제만 안 되고 보관은 되던 게 그 차이였다. 확인은 화면 안에서 두 단계로 받는다.

**쓰기에는 언제나 `.select()` 를 붙인다.** RLS 가 막으면 Postgres 는 오류를 내지 않고
0건을 처리하고 supabase-js 는 성공으로 돌려준다. 바뀐 줄 수를 세고 0이면 그렇다고 말한다.

**날짜는 UTC 자정으로 다룬다.** `new Date('2026-09-06T00:00:00')` 은 한국 시각 자정이라
`toISOString()` 이 하루 앞으로 민다. 여행 첫날 앞에 유령 칩이 생겼다. `asDate()` 를 쓴다.

**"A는 되는데 B만 안 된다"는 A와 B의 차이를 먼저 본다.** 짐작으로 스키마를 의심했다가
틀렸다. 진단 SQL 을 만들어 재고 나서 고칠 것.

**새 칸을 읽는 화면 코드를 마이그레이션보다 먼저 올리지 않는다.** 없는 칸을 요청하면
질의가 통째로 실패해서 목록이 아예 안 나온다. 도시 313곳이 한 번 통째로 사라졌다.
지금은 없는 칸을 빼고 다시 물어보게 해뒀지만, 순서를 지키는 것이 먼저다.

**안 보이는 자리에 오류를 적지 않는다.** 그 사고 때 오류가 다른 탭의 오류칸에 적혀서
화면만 비어 보였다. 원인 찾기가 훨씬 오래 걸렸다.

**Postgres 는 함수를 만들면 PUBLIC 에게 실행 권한을 준다.** `anon`·`authenticated`
에서만 빼면 그대로 부를 수 있다. 잠글 함수는 `revoke ... from public` 을 먼저 한다.

**위키백과 API 는 빠르게 부르면 429 로 막는다.** 300곳을 받으려면 요청 사이에
0.5~0.9초를 둔다. 막히면 "없는 것"처럼 보여서 잘못 판단하기 쉽다.
- [ ] Storage 버킷 `trip-files` + 정책
- [ ] 로그인 · 여행 목록 · 새 여행 · 초대 코드
- [ ] 일정 화면 (도쿄 앱에서 이식)
- [ ] 실시간 구독 — 8초 폴링을 대체. `REV` 는 사라진다
- [ ] 오프라인: 캐시 우선 SW + 쓰기 큐 이식
- [ ] AI 는 Edge Function 뒤로. **키를 클라이언트에 절대 두지 않는다**
- [ ] 지출 정산 · 후보 투표 · 읽기 전용 공유 링크
- [ ] 일정 초안 생성 (문서 "새로 만들 것") — P 를 끌어오는 핵심

## 도쿄 앱에서 가져올 것

- 오프라인 구조 (`../pwa/sw.js`) — 캐시 우선 네비게이션이 핵심. 부팅 30ms
- **오프라인 쓰기 큐** — 이미 만들어 검증까지 끝났다(2026-08-02).
  마이그레이션 문서 §5 는 "처음엔 읽기만"이라고 적었는데 그건 만들기 전 얘기다
- 낙관적 저장 3종: dirty 플래그, 디바운스, 응답 번호로 늦은 응답 버리기
- AI 응답 계약 `{reply, places, actions}` 과 안전장치 다섯 가지 (HANDOFF 5장)
- AI 제안 카드 구조 (후보·예약·지출을 카드로 내고 사용자가 고르게)
- 블로그 링크에서 장소 뽑기. 네이버는 차단이라 **화면 캡처 전송**이 현실적인 답
- 디자인 토큰과 부품 (유리 알약 상단바, 시트, 카드), 글자 배율 `--ts`
- 이동시간 모델 — 이제 상수가 아니라 `trips` 행에서 읽는다
  - 도보: `km × walk_min_per_km + walk_base_min` (도쿄 12, 2)
  - 교통: `km × transit_factor + transit_base_min` (도쿄 3.2, 12)
  - 임계: `walk_max_km` (도쿄 1.3)

## 이동시간을 왜 직접 계산하는가 (2026-08-02에 한 번 다시 물었던 것)

**"구글지도로 다 되는데 왜 어림값을 두나"** — 둘이 하는 일이 다르다.
구글지도는 "어떻게 가지"에 답하고, 이 계산은 "이 하루가 물리적으로 가능한가"에 답한다.

- **링크로는 숫자가 앱에 안 들어온다.** 지금 붙이는 건 구글지도 링크라 사용자 눈에만 보인다.
  앱이 숫자를 알려면 Directions API를 불러야 하고 그건 요청마다 과금된다.
- **한 번이 아니다.** 4일 × 하루 5개면 연속 쌍이 16개. 일정을 하나 옮길 때마다 다시 재야 한다.
  문서가 정한 흐름이 "직접 고쳤을 때 → 계산 검사 → 알림, **AI 호출 0회**"인데
  **공짜라서 매번 돌릴 수 있는 구조다.** 유료 API로 바꾸면 매번 못 돌린다.
- 매번 못 돌리면 "체크인이 15시인데 11시 35분에 잡혀 있다"를 못 잡는다.
  문서 8-2의 결론이 "남들은 생성만 하고 검증을 안 한다, 거기가 빈자리"였고 그 빈자리를 메우는 게 이거다.
- 비행기모드에서도 돈다.

**정확도는 낮은 게 맞다.** 직선거리 어림이고 4등급 중 `dense`만 실측이다.
그래서 **어림값은 빡빡한지 판단하는 데만 쓰고, 정확한 시간은 구글지도 링크에 맡긴다.**
화면에도 "약 25분"처럼 어림임을 드러낸다.

## 조심할 것

**RLS 는 켜는 걸 빠뜨리면 전 사용자 데이터가 열린다.** 새 테이블마다
`enable row level security` + 정책 두 줄을 같은 자리에서 쓴다. 001의 20번 블록이 그 틀.

**`trip_members` 정책이 `trip_members` 를 조회하면 무한 재귀(42P17)로 전부 죽는다.**
멤버십 판정은 `can_read_trip` / `can_write_trip` / `trip_role_of` SECURITY DEFINER
함수로만 한다. 001의 7번 블록.

**읽기 전용 공유는 RLS 로 익명 접근을 열지 않는다.** 실수 한 번에 지출·예약번호가
다 샌다. `get_shared_trip()` 함수가 내보낼 필드를 손으로 고른다. 001의 22번 블록.

**GitHub Pages 배포는 몰아서 한 번만.** 짧은 시간에 여러 번 밀면 진행 중이던
배포를 취소시켜 연쇄로 다 죽는다.

**PowerShell 5.1 은 BOM 없는 UTF-8 `.ps1` 을 ANSI 로 읽는다.** 스크립트는 ASCII 로만.
