# 여기에는 비밀이 들어가면 안 됩니다. 공개 저장소입니다.
# anon key 는 공개돼도 되지만(RLS 가 지킵니다) service_role key 는 절대 안 됩니다.
.env
.env.*
*.local
secrets*
service-role*

# 편집기 · OS
.vscode/
.idea/
Thumbs.db
desktop.ini
_maptest.html
# 디자인 볼 때 쓰는 목업. 로그인 없이 홈을 그려보고 폰 크기로 가둡니다.
# `_mock.html?before` 는 `_before.css`(= git show HEAD:app.css) 를 씁니다.
_mock.html
_frame.html
_before.css
_hello_before.html

tools/fonts/
